export class Event {
    id: number;
    name: string;
    type: string; // corresponds to the appropriate event type (workshop, activity, food, etc)
    location: number; // corresponds to the appropriate event location (room, library)
}
